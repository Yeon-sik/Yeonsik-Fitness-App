apply plugin: "org.jetbrains.kotlin.multiplatform"
apply plugin: "com.android.kotlin.multiplatform.library"

kotlin {
    androidLibrary {
        namespace = "com.yeonsik.fitness.shared"
        compileSdk = 36
        minSdk = 26

        withHostTestBuilder {}.configure {}
    }

    iosX64()
    iosArm64()
    iosSimulatorArm64()

    sourceSets {
        commonTest {
            dependencies {
                implementation kotlin("test")
            }
        }
    }
}
